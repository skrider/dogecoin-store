import React from 'react';
export type FilterBankProps = {};

const FilterBank: React.FC<FilterBankProps> = ({}: FilterBankProps) => {
 return (
  <div>
    
  </div>
 );
};

export default FilterBank;